#!/bin/bash

PIDFILE="/tmp/ai-checkin.pid"
if [ -f "$PIDFILE" ]; then
  PID=$(cat "$PIDFILE")
  kill "$PID" 2>/dev/null || true
  rm -f "$PIDFILE"
fi
echo "[uninstall_callback] ai-checkin stopped."
exit 0
